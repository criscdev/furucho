# ===========================================
# GitHub Actions CI/CD for Furucho Frontend
# ===========================================
# Builds, tests, and deploys the React frontend

name: Frontend CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'src/**'
      - 'package.json'
      - 'vite.config.ts'
      - '.github/workflows/frontend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'app/**'
      - 'src/**'
      - 'package.json'

jobs:
  # ===========================================
  # Build and Test
  # ===========================================
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run type check
        run: npm run typecheck

      - name: Run unit tests
        run: npx vitest run

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        run: npx playwright test --project=chromium

      - name: Build for production
        run: npm run build

      - name: Upload build artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: build

  # ===========================================
  # Deploy to DigitalOcean App Platform
  # ===========================================
  # PREREQUISITES: Configure these secrets in GitHub repo settings:
  #   - DIGITALOCEAN_ACCESS_TOKEN: Your DO API token
  #   - DIGITALOCEAN_APP_ID: Your App Platform app ID
  # Also create 'production' environment in Settings > Environments
  # ===========================================
  # deploy-digitalocean:
  #   needs: build
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: production
  #     url: https://robertafurucho.com
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Install doctl
  #       uses: digitalocean/action-doctl@v2
  #       with:
  #         token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
  #
  #     - name: Deploy to DigitalOcean App Platform
  #       run: doctl apps create-deployment ${{ secrets.DIGITALOCEAN_APP_ID }}
