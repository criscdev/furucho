# ===========================================
# GitHub Actions CI/CD for Furucho API
# ===========================================
# Builds and tests the Java backend, then deploys to Azure App Service

name: Backend CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'backend/**'

defaults:
  run:
    working-directory: backend

jobs:
  # ===========================================
  # Build and Test
  # ===========================================
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: ./mvnw clean verify -B

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: backend/target/surefire-reports

      - name: Upload JAR artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: app-jar
          path: backend/target/*.jar

  # ===========================================
  # Deploy to Azure App Service
  # ===========================================
  # PREREQUISITES: Configure these secrets in GitHub repo settings:
  #   - AZURE_CREDENTIALS: Azure service principal JSON
  #   - AZURE_WEBAPP_NAME: Name of your Azure Web App
  # Also create 'production' environment in Settings > Environments
  # ===========================================
  # deploy-azure:
  #   needs: build
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: production
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up JDK 21
  #       uses: actions/setup-java@v4
  #       with:
  #         java-version: '21'
  #         distribution: 'temurin'
  #         cache: 'maven'
  #
  #     - name: Build with Maven
  #       run: ./mvnw clean package -DskipTests -B
  #       working-directory: backend
  #
  #     - name: Login to Azure
  #       uses: azure/login@v2
  #       with:
  #         creds: ${{ secrets.AZURE_CREDENTIALS }}
  #
  #     - name: Deploy to Azure App Service
  #       uses: azure/webapps-deploy@v3
  #       with:
  #         app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
  #         package: backend/target/*.jar
  #
  #     - name: Logout from Azure
  #       run: az logout
